# HERO.IO — Productive Apps (Perfect Design)

A responsive React SPA, matching your screenshot design: centered hero, purple KPI band, trending grid, app details with chart and install, and custom errors. LocalStorage installation & uninstall included.

## Run
```bash
npm i
npm run dev
```
## Build
```bash
npm run build
npm run preview
```
## Deploy
- Netlify / Cloudflare Pages: `_redirects` included
- Vercel: `vercel.json` rewrites included
